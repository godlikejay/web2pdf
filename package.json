{
  "name": "web2pdf",
  "version": "1.0.0",
  "description": "HTTP Server for generate PDFs from URLs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "puppeteer": "^24.34.0"
  }
}
